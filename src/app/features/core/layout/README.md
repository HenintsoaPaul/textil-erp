# Admin Layout
This is the default admin panel.
It is applied by default to all authenticated pages.

## Future evolution
The should be able to configure user sidebars and topbars dynamically based on connected user's role and permissions.
User's panel template would be stored in the backend.